/* Write your global styles here, in PostCSS syntax */

@font-face {
  font-family: 'Noto Serif';
  src: url(../NotoSerif-VariableFont_wdth,wght.FontimizeSubset.woff2);
}

@tailwind base;


:root {
  font-family: 'Noto Serif', serif;
  /* font-family: Helvetica, Arial, sans-serif; */
  font-size: 16px;
  line-height: 24px;
  font-weight: 400;

  color-scheme: light;
  color: #213547;
  /* background-color: #242424; */

  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  -webkit-text-size-adjust: 100%;
}

body {
  margin: 4;
  display: flex;
  place-items: center;
  min-width: 320px;
  min-height: 100vh;
}

h1 {
  font-size: 3.2em;
  line-height: 1.1;
}

/* replace the whole #app block with this */
#app {
  /* desktop / tablet  -------------------- */
  max-width: 1280px;     /* centred column, never hugs left */
  margin: 0 auto;        /* equal left- and right-margins   */
  box-sizing: border-box;
  text-align: center;
  padding-inline: 1rem;  /* small gutters inside the column */

  /* mobile  ------------------------------ */
}

@media (max-width: 767px) {
  #app {
    width: 100%;         /* fill the phone, keeps no-scroll fix */
    padding-inline: 0;   /* optional: drop side padding on phones */
  }
}

/* button {
  border-radius: 8px;
  border: 1px solid transparent;
  padding: 0.6em 1.2em;
  font-size: 1em;
  font-weight: 500;
  font-family: inherit;
  background-color: #1a1a1a;
  cursor: pointer;
  transition: border-color 0.25s;
}
button:hover {
  border-color: #e07ca9;
}
button:focus,
button:focus-visible {
  outline: 4px auto #c4005a;
} */

@media (prefers-color-scheme: light) {
  :root {
    color: #213547;
  }
  a:hover {
    color: #747bff;
  }
  button {
    background-color: #f9f9f9;
  }
}

@tailwind components;

@tailwind utilities;
